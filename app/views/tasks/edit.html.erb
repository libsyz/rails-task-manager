<h3> Edit Task <%= @task.name   %> </h3>



<%= form_for(@task) do |t| %>
  <%= t.label :name %>
  <%= t.text_field :name %>

  <%= t.label :description %>
  <%= t.text_field :description %>

  <%= t.label :done %>
  <%= t.check_box :done %>

  <%= t.submit %>
<% end %>

